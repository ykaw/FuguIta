srcdirs /opt/fi/7.4/lib/special
srcdirs /usr/src/distrib/special

progs init ksh ln disklabel mount mount_ffs mount_cd9660 mount_msdos mount_ntfs mount_ext2fs mount_vnd vnconfig umount newfs sed sleep sysctl swapctl reboot

ln ksh sh
ln ksh -sh
ln swapctl swapon
ln reboot halt
ln newfs mount_mfs

#libs -lutil -lotermcap -ll -lm -lcrypto
libs -lutil
