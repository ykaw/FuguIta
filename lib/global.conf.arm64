# for arm64 / Raspberry Pi 3
#
disable_legacyboot=Yes
disk_head_gap=32768    # in sectors
fuguita_uefi_kb=16384  # in kilobytes
fuguita_sys_mb=962     # in megabytes
memfstype=mfs          # 'mfs' or 'tmpfs'
mfs_max_mb=30720       # mfs hard limit - 30GB
