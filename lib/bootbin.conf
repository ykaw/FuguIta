#
# This file is automatically generated by `makeconf'
#

srcdirs /usr/src/distrib/special
srcdirs /usr/src/bin /usr/src/sbin /usr/src/usr.bin /usr/src/usr.sbin
srcdirs /usr/src/sys/arch/i386/stand

#progs init ksh ln mount mount_ffs mount_cd9660 newfs sysctl
progs ksh ln mount mount_ffs mount_cd9660 mount_msdos mount_ntfs mount_vnd umount newfs sleep

ln ksh sh
ln ksh -sh
ln newfs mount_mfs
ln mount_vnd vnconfig

# libs -lstubs -lutil -lotermcap -ll -lm -ldes
# libs -lutil -lotermcap -ll -lm -ldes -lcrypto
libs -lutil -lotermcap -ll -lm -lcrypto

# libdirs distrib/special/libstubs
